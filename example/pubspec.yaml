name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  score_progress_pretty_display: 0.0.1

dependency_overrides: 
  score_progress_pretty_display:
    path: ../

flutter:
  uses-material-design: true
